library('RPostgreSQL')
#create connection object
con <- dbConnect(drv =PostgreSQL(),
user="postgres",
password="password",
host="localhost",
port=5432,
dbname="postgres")
library('RPostgreSQL')
#create connection object
con <- dbConnect(drv =PostgreSQL(),
user="postgres",
password="password",
host="localhost",
port=5432,
dbname="postgres")
library('RPostgreSQL')
#create connection object
con <- dbConnect(drv =PostgreSQL(),
user="postgres",
password="password",
host="localhost",
port=5432,
dbname="postgres")
library('RPostgreSQL')
#create connection object
con <- dbConnect(drv =PostgreSQL(),
user="postgres",
password="password",
host="localhost",
port=5432,
dbname="postgres")
library('RPostgreSQL')
#create connection object
con <- dbConnect(drv =PostgreSQL(),
user="postgres",
password="password",
host="localhost",
port=5432,
dbname="postgres")
library('RPostgreSQL')
#create connection object
con <- dbConnect(drv =PostgreSQL(),
user="postgres",
password="password",
host="localhost",
port=5432,
dbname="postgres")
library('RPostgreSQL')
#create connection object
con <- dbConnect(drv =PostgreSQL(),
user="postgres",
password="password",
host="localhost",
port=5432,
dbname="postgres")
install.packages("RPostgres")
library('RPostgres')
#create connection object
con <- dbConnect(drv =PostgreSQL(),
user="postgres",
password="password",
host="localhost",
port=5432,
dbname="postgres")
library(DBI)
# Connect to the default postgres database
con <- dbConnect(RPostgres::Postgres())
library(DBI)
# Connect to a specific postgres database i.e. Heroku
con <- dbConnect(RPostgres::Postgres(),
dbname = 'postgres',
host = 'localhost',
port = 5432,
user = 'postgres',
password = 'password')
dbListTables(con)
res <- dbSendQuery(con, "SELECT * FROM PLAYER p
JOIN PLAYER_STATS ps ON p.player_id = ps.player_id")
dbFetch(res)
res <- dbSendQuery(con, "SELECT * FROM PLAYER p")
dbFetch(res)
res <- dbSendQuery(con, "SELECT * FROM PLAYER p")
dbFetch(res)
dbClearResult(res)
res <- dbSendQuery(con, "SELECT * FROM PLAYER p")
dbFetch(res)
dbClearResult(res)
res <- dbSendQuery(con, "SELECT * FROM PLAYER_STATS p")
dbFetch(res)
dbClearResult(res)
res <- dbSendQuery(con, "SELECT * FROM PLAYER_STATS JOIN PLAYER")
res <- dbSendQuery(con, "SELECT * FROM PLAYER_STATS JOIN PLAYER ON player_id")
res <- dbSendQuery(con, "SELECT * FROM PLAYER_STATS JOIN PLAYER ON player_id = player_id")
res <- dbSendQuery(con, "SELECT * FROM PLAYER_STATS ps JOIN PLAYER p ON ps.player_id = p.player_id")
dbFetch(res)
dbClearResult(res)
res <- dbSendQuery(con, "SELECT *
FROM PLAYER p
JOIN PLAYER_STATS ps ON p.player_id = ps.player_id")
res <- dbSendQuery(con, "SELECT * FROM PLAYER p JOIN PLAYER_STATS ps ON p.player_id = ps.player_id")
res <- dbSendQuery(con, "SELECT * FROM PLAYER p JOIN PLAYER_STATS ps ON p.player_id = ps.player_id")
dbFetch(res)
dbClearResult(res)
res <- dbSendQuery(con, "SELECT * FROM PLAYER p JOIN PLAYER_STATS ps ON p.player_id = ps.player_id")
dbFetch(res)
dbClearResult(res)
res <- dbSendQuery(con, "SELECT * FROM PLAYER p JOIN PLAYER_STATS ps ON p.player_id = ps.player_id")
dbFetch(res)
dbClearResult(res)
res <- dbSendQuery(con, "SELECT * FROM PLAYER p JOIN PLAYER_STATS ps ON p.player_id = ps.player_id
JOIN PLAYER_RATINGS pr ON p.player_id = pr.player_id
WHERE ps.season = 22 and pr.season = 23")
dbFetch(res)
dbClearResult(res)
